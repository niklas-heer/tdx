{
  "name": "tdx",
  "version": "0.2.1",
  "description": "Your todos, in markdown, at terminal speed",
  "type": "module",
  "main": "src/cli.ts",
  "scripts": {
    "dev": "bun run src/cli.ts",
    "build": "bun build --compile --minify src/cli.ts --outfile tdx",
    "test": "bun test"
  },
  "dependencies": {
    "ink": "^4.4.1",
    "react": "^18.2.0",
    "chalk": "^5.3.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/react": "^18.2.0",
    "react-devtools-core": "^7.0.1"
  },
  "bin": {
    "tdx": "./tdx"
  }
}
